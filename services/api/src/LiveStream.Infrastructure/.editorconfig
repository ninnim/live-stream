# EF Core generates migration files with its own formatting. They are regenerated by tooling, so
# project style rules are not applied to them rather than hand-editing after every scaffold.
[Persistence/Migrations/*.cs]
generated_code = true
dotnet_diagnostic.IDE0161.severity = none
dotnet_diagnostic.IDE0005.severity = none
dotnet_analyzer_diagnostic.severity = none
